# Base URL the SPA uses for API calls. Defaults to "/api" (relative), which the
# Vite dev proxy, nginx, and the cloud load balancers all route to the backend.
# Override only if you serve the API from a different origin.
VITE_API_BASE_URL=/api
